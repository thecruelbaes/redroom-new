# RedRoom Studio — лендинг

Продающий лендинг школы музыки в Новороссийске. Фокус — индивидуальные уроки барабанов и
гитары (вторично: репетиционная точка, звукозапись). Стек: **Next.js 15 + Tailwind v4**.

## Запуск

```bash
npm install
npm run dev      # http://localhost:3000
npm run build    # прод-сборка
```

## Главный оффер

«Подпишись на Telegram-канал [@redroomstudio](https://t.me/redroomstudio) → первое занятие
бесплатно». Этот хук — в hero, в большом CTA-блоке (`#trial`) и в плавающей кнопке на мобиле.

## Telegram-бот заявок

Форма (`#contact`) шлёт заявку в `POST /api/lead`, оттуда — в Telegram (как в Bronx).
Переменные окружения (см. `.env.example`):

- `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
- `TELEGRAM_CHAT_ID` — куда слать заявки (личка/группа)
- `NEXT_PUBLIC_SITE_URL` — прод-URL для sitemap/robots/canonical
- `NEXT_PUBLIC_YANDEX_VERIFICATION` — код Яндекс.Вебмастера (необязательно)

Без токена форма работает в dev-режиме: заявка логируется в консоль и возвращает успех
(лид не теряется при разработке). ⚠️ В production это означает, что заявки реально теряются —
без предупреждения владельцу, кроме серверного лога. Убедись, что оба токена заданы перед сдачей.
Rate-limit 5 заявок/мин с IP, ввод экранируется, honeypot.

## Админка и Supabase

Обязательные переменные для `/admin` и хранения отзывов (иначе — см. предупреждения ниже):

- `ADMIN_PASSWORD` — пароль входа в `/admin`. **Обязателен в production** — без него сборка
  падает при старте (см. `lib/auth.ts`), чтобы админка не оказалась защищена дефолтным паролем
  из публичного репозитория.
- `ADMIN_SESSION_SECRET` — секрет для подписи сессионной куки. Тоже обязателен в production.
- `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` — публичный клиент (чтение
  опубликованных отзывов).
- `SUPABASE_SERVICE_ROLE_KEY` — серверный ключ для записи (создание/удаление/скрытие отзывов).
  Никогда не должен попасть в клиентский бандл — используется только через `server-only` модули.

## Контент

Весь контент — в [`lib/content.ts`](lib/content.ts). Места `// TODO` нужно заменить
реальными данными перед сдачей:

- **Телефон** студии (сейчас плейсхолдер `+7 (000) ...`).
- **Адрес** студии в Новороссийске.
- **Био преподавателей** (Олег, Владимир — барабаны; Сергей — гитара) — сейчас реалистичные заглушки.
- **Отзывы** — сейчас заглушки, заменить на реальные (Яндекс/2ГИС/скрины).
- **Статистика** (лет/учеников) — проверить цифры.
- **Privacy** — дополнить реквизитами ИП (ИНН, ОГРНИП, e-mail). Оператор: ИП Шубный Сергей Александрович.

## Дизайн

Cinematic dark: OLED-чёрный `#0A0A0B` + кроваво-красный `#E10600` + фиолетовый сценический
`#A855F7`. Шрифты Oswald (заголовки) + Inter (текст), оба с кириллицей. Подробно — в
[`DESIGN.md`](DESIGN.md). Изображения-оригиналы в `images/`, рабочие — в `public/images/`.

> ⚠️ `public/images/teacher-sergei.jpg` — 6.7 МБ. `next/image` оптимизирует на отдаче, но перед
> продом исходник лучше пересжать (≤ ~400 КБ).

## Секции

Navbar · Hero (параллакс + хук) · Услуги (4 карточки) · Почему мы + статистика ·
Преподаватели · Студия (галерея) · Free-trial CTA · Отзывы · Форма заявки · Footer · /privacy.
